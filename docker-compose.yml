version: '3.8'

services:
  app:
    build:
      context: .
    volumes:
      - ./:/app
    restart: always
    environment:
      TGBOT_TOKEN: "${TGBOT_TOKEN}"5174579630:AAEqCAh44zqb05Ebbb7YGRHynbDMlxLfM0o
      DB_PWD: "${DB_PWD}"

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: "${DB_PWD}"

    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
